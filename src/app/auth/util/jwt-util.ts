// import jwt_decode from 'jwt-decode';

export function isExpired(token: string): boolean {
    return false;
//   const decoded = jwt_decode(token);
//   const exp = decoded.exp;
//   const expired = exp < new Date().getTime() / 1000;
//   return expired;
}